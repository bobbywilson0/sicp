;The evaluted if statement returns a plus or minus operator that is then 
;applied to 'b'. The expression then uses subtraction if 'b' is negative
;to or addition if 'b' is positive affectively making 'b' the absolute
;value.
